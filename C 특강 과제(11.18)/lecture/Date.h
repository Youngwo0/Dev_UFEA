#pragma once
#include<math.h>
long get_year(long Date);
long get_month(long Date);
long get_day(long Date);

long day_count(long start_date, long end_date);

//struct Student {
//	long student_id;
//	long age;
//	char* name;
//	char* department;
//	char* major;
//};
//
//struct Position {
//	double x = 0;
//	double y = 0;
//};
//
//double distance(struct Position pos1, struct Position pos2)
//{
//	return sqrt(pow(pos1.x - pos2.x, 2) + pow(pos1.y - pos2.y, 2));
//}

